# 实验验证与结果分析

## 5.1 实验环境

### 5.1.1 系统部署架构

本实验采用分布式部署架构，包含以下组件：

| 组件 | 技术栈 | 部署位置 | 功能 |
|------|--------|----------|------|
| 管理端 (systemwire2) | Python Flask + gRPC | 管理服务器 | Web管理界面、命令下发 |
| 探针端 (agent-go) | Go语言 | 靶机 | 蜜点部署、日志采集 |
| 告警服务 (alert_server) | Go + Gin | 管理服务器 | 文件蜜点告警接收 |
| SSH蜜罐 (ssh-vpn) | Go | 靶机 | 账户蜜点 |

### 5.1.2 实验数据

本实验模拟了一个完整的APT攻击场景，包含7个攻击阶段：

| 阶段 | 攻击行为 | 告警类型 | 告警数量 |
|------|----------|----------|----------|
| 侦察 | SSH暴力破解尝试 | 账户蜜点 | 3 |
| 初始访问 | 文件蜜点触发 | 文件蜜点 | 3 |
| 命令执行 | 系统命令执行 | audit日志 | 7 |
| 持久化 | 创建后门 | audit日志 | 4 |
| 横向移动 | 尝试访问其他主机 | 账户蜜点 | 2 |
| 数据外泄 | 网络连接外传 | audit日志 | 3 |
| 爬虫检测 | 寄生蜜点触发 | 寄生蜜点 | 2 |

**总计**: 24条告警数据

## 5.2 实验流程

### 5.2.1 阶段一：多源告警数据汇聚

**目标**: 将异构的蜜点告警数据转换为统一格式

**方法**: 采用适配器模式，为每种告警类型实现专用适配器

**数据结构**:
```python
UnifiedAlert {
    alert_id: str           # 唯一标识
    alert_type: AlertType   # file/account/parasitic/audit
    timestamp: datetime     # 时间戳
    attacker_ip: str        # 攻击者IP
    action: str             # 动作类型
    target_path: str        # 目标路径
    process_info: dict      # 进程信息
    details: dict           # 原始详情
}
```

**结果**: 成功汇聚24条告警，类型分布为account:5, file:3, audit:14, parasitic:2

### 5.2.2 阶段二：因果溯源图构建

**目标**: 将告警数据转换为Subject→Action→Object的有向因果图

**方法**: 
1. 实体识别：从告警中提取网络地址、文件、进程三类实体
2. 关系抽取：根据动作类型建立实体间的因果关系
3. 图构建：使用NetworkX构建有向无环图

**转换规则**:

| 告警类型 | Subject | Action | Object |
|---------|---------|--------|--------|
| 账户蜜点 | network:攻击者IP | ssh_login | network:目标主机 |
| 文件蜜点 | network:攻击者IP | file_access | file:目标路径 |
| audit-execve | process:父PID | execve | process:子PID |
| audit-openat | process:PID | openat | file:文件路径 |
| audit-connect | process:PID | connect | network:目标地址 |
| 寄生蜜点 | network:攻击者IP | url_access | file:蜜点URL |

**结果**:
- 节点数: 24 (network:5, file:8, process:11)
- 边数: 23
- 三元组数: 23
- 边类型分布: ssh_login:5, file_access:3, execve:6, openat:1, read:2, write:2, connect:2, url_access:2

### 5.2.3 阶段三：DQN图谱自适应裁剪

**目标**: 使用深度强化学习去除因果图中的噪声边，保留核心攻击链路

**模型架构**:
```
输入: 节点特征 (N, 16) + 边索引 (2, E)
        ↓
┌─────────────────────────┐
│  GAT编码器 (4层)         │
│  - 残差连接              │
│  - LayerNorm             │
│  - 多头注意力 (4 heads)  │
└─────────────────────────┘
        ↓
    节点嵌入 (N, 64)
        ↓
┌─────────────────────────┐
│  边嵌入拼接              │
│  [h_src ∥ h_dst] → (E, 128) │
└─────────────────────────┘
        ↓
┌─────────────────────────┐
│  Q-Network (MLP)         │
│  128 → 256 → 128 → 64 → 2 │
└─────────────────────────┘
        ↓
    [Q_keep, Q_prune] (E, 2)
        ↓
    决策: argmax(Q) → 保留/裁剪
```

**奖励函数**:

| 情况 | 奖励 | 含义 |
|------|------|------|
| 正确保留核心边 | +5.0 | 保持攻击链完整性 |
| 正确裁剪冗余边 | +2.0 | 去除噪声 |
| 错误裁剪核心边 | -10.0 | 攻击链断裂（严重惩罚） |
| 错误保留冗余边 | -2.0 | 噪声未清除 |

**训练参数**:
- 学习率: 5e-4
- 训练轮数: 100 epochs
- 早停耐心: 30 epochs
- Focal Loss: α=0.6, γ=2.0

**实验结果**:

| 指标 | 数值 | 说明 |
|------|------|------|
| 准确率 (Accuracy) | 0.7333 | 整体决策正确率 |
| 精确率 (Precision) | 0.7033 | 裁剪边中正确的比例 |
| 召回率 (Recall) | 0.4660 | 冗余边被识别的比例 |
| F1分数 | 0.5523 | 精确率和召回率的调和平均 |
| **核心边召回率** | **0.8934** | **核心攻击边被保留的比例** |
| 图谱压缩率 | 23.8% | 冗余边被裁剪的比例 |

**分析**:
- 核心边召回率达到89.34%，说明模型能够有效识别并保留关键攻击路径
- 图谱压缩率为23.8%，成功去除了部分噪声边
- F1分数为0.5523，表明在精确率和召回率之间取得了较好的平衡

### 5.2.4 阶段四：Graph-to-Text转换

**目标**: 将裁剪后的因果图转换为LLM可理解的叙述性文本

**方法**: 
1. 概述生成：统计节点和边的类型分布
2. 时间线生成：按时间排序攻击行为
3. 阶段分析：映射到MITRE ATT&CK框架
4. 关键路径：识别攻击链路
5. 技术细节：提取系统调用信息

**结果**: 成功生成3种类型的提示词
- 意图分析提示词: 2438字符
- TTP映射提示词: 2405字符
- 事件报告提示词: 2355字符

## 5.3 创新点分析

### 5.3.1 多源异构数据融合

本系统创新性地将四种不同类型的蜜点告警（文件蜜点、账户蜜点、寄生蜜点、audit日志）通过适配器模式进行统一汇聚，解决了传统蜜罐系统数据孤岛的问题。

### 5.3.2 因果图建模

将离散的告警事件转换为结构化的因果溯源图，保留了攻击行为之间的时序和因果关系，为后续的意图推理提供了结构化输入。

### 5.3.3 DQN自适应裁剪

引入深度强化学习进行图谱裁剪，相比传统规则方法：
- 能够自适应地学习噪声边的特征
- 通过奖励函数平衡核心边保留和噪声去除
- 核心边召回率达到89.34%，保证攻击链完整性  

### 5.3.4 Graph-to-Text转换

将图结构转换为自然语言描述，解决了LLM无法直接处理图结构数据的问题，实现了从结构化数据到自然语言的桥接。

## 5.4 与现有工作的对比

| 特性 | 传统蜜罐 | 本系统 |
|------|----------|--------|
| 蜜点类型 | 单一 | 多源（文件+账户+寄生+审计） |
| 数据格式 | 异构 | 统一 |
| 图谱构建 | 无 | 因果溯源图 |
| 噪声处理 | 规则过滤 | DQN自适应裁剪 |
| 意图推理 | 无 | LLM + Graph-to-Text |

## 5.5 本章小结

本章通过端到端实验验证了系统的完整性和有效性：
1. 成功汇聚24条多源告警数据
2. 构建了包含24个节点、23条边的因果溯源图
3. DQN模型达到89.34%的核心边召回率和23.8%的压缩率
4. 成功生成可用于LLM推理的结构化提示词

实验结果表明，本系统能够有效地从海量告警中提取关键攻击路径，为后续的LLM意图推理提供高质量输入。
