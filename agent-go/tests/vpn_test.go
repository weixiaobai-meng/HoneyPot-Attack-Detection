// vpn_config_test.go
package tests

import (
	"testing"
)

func TestVPNConfig_CreateFakeConfig(t *testing.T) {
	// // 创建一个临时目录
	// tempDir := t.TempDir()

	// // 创建 VPNConfig 实例
	// vpnConfig := &VPNConfig{
	// 	RemoteIP: "192.168.1.1",
	// 	Path:     "D:\\test", // 配置文件保存路径
	// }

	// // 创建虚假的配置
	// filePath, err := vpnConfig.createFakeConfig(tempDir)
	// assert.NoError(t, err, "VPN config creation failed")

	// // 检查生成的配置文件是否存在
	// _, err = os.Stat(filePath)
	// assert.NoError(t, err, "VPN config file not created")

}
